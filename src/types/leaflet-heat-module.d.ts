// leaflet.heat ships no types and is imported only for its side effect of
// augmenting the Leaflet namespace. This file must stay import-free so the
// declaration remains ambient.
declare module "leaflet.heat";
