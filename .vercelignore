# The legacy Python app. Still in the repo until the cutover, but nothing
# in the Next.js build imports it, so there is no reason to upload it.
backend/
frontend/

# Developer-only scripts (one-time data migration and photo recovery).
# They run locally against the database, never on Vercel.
scripts/
